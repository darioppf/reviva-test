default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    kernel: '@kernel'
    extensions:
        Behat\Symfony2Extension:
          kernel:
            bootstrap: features/bootstrap/FeatureContext.php
            class: App\Kernel
